/** Client-safe Odoo documents explorer constants (no server-only imports). */

export const ODOO_VIRTUAL_ALL_ATTACHMENTS_FOLDER_ID = 0;

export type OdooDocumentsExplorerMode = "documents_app" | "virtual_folders" | "attachments_only";
